{
  "name": "jintechbot",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "pm2 stop maker && pm2 start app.js --name maker && pm2 logs"
  },
  "author": "@sowmay_jain",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.2.0",
    "telegraf": "^3.25.5"
  }
}
